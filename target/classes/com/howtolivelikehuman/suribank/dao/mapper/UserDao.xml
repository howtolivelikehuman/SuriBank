<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	<!-- login 쿼리 (1개) -->
	<select id="login" parameterType="com.howtolivelikehuman.suribank.dto.UserVO" resultType="com.howtolivelikehuman.suribank.dto.UserVO">
		select id, name, nickname from user where id=#{id} and password=#{password}
	</select>
	
	<!--  insert 쿼리 -->
	<insert id="signup" parameterType="com.howtolivelikehuman.suribank.dto.UserVO">
		insert into user (id, password, name, nickname, email) values ( #{id}, #{password}, #{name}, #{nickname} , #{email})
	</insert>

</mapper>